require "spec_helper"

describe GalaxyCalculator do
  it "has a version number" do
    expect(GalaxyCalculator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
